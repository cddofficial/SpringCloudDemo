server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cddofficial/config-repo-demo  #公用的git配置
          search-paths:          - foo   #进入config-repo-demo仓库的foo目录
          - bar   #进入config-repo-demo仓库的bar目录